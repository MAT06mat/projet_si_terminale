#:import AppBar ui.app_bar

<LoadMenu>:
    orientation: 'vertical'
    AppBar:
        title: "Load cube"
    Saves:
        id: saves
        orientation: 'vertical'
        adaptive_height: True
    Widget:


<Save>:
    MDListItemHeadlineText:
        text: root.name
